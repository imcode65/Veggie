// export default function(context){

//   if(process.browser && window.innerWidth < 1024){     
//     // return
//   }
// }
export default function({app}){
  // app.router.beforeResolve((to, from, next) => {
  //   if(from.name == null){
  //   }
  // })
  //     // route.matched[0].beforeEnter (to, from, next) {
  //     // }
  // if(process.browser && window.innerWidth < 1024){     
  //   // return
  // }
}
